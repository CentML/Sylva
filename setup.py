import setuptools

setuptools.setup(
    name="sylva",
    version="0.1.0",
    description="Sylva: boost fine-tuning performance via sparse embedded adapters and hierarchical approximate second-order information.",
    url="https://github.com/CentML/Sylva",
    packages=setuptools.find_packages(),
)
